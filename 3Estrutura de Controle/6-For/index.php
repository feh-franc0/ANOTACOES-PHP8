<?php
/**FOR
 * O comando for repete um trecho de código com base em 3 expressões que definimos em seus parênteses.
 * 1.Define o início da repetição
 * 2.Define a condição que será avaliada para as repetições.
 * 3.Define o que será feito no final de cada repetição.
 * 
 * ultiliamos mt o for para percorrer arrays e coleções.
 */

 for ($contador=0; $contador <= 10 ; $contador++) { 
    var_dump($contador);
 }